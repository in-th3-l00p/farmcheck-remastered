import { StyleSheet, View } from "react-native";

interface SensorElementProps {
    sensor: any;
    data?: any;
}

const SensorElement = ({ sensor, data }: SensorElementProps) => {
    return <View></View>;
};

const styles = StyleSheet.create({});

export default SensorElement;
