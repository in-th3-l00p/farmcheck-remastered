// Post login user data
export const PostLoginURL = "/api/v1/auth/login";

// Post register user data
export const PostRegisterURL = "/api/v1/user/register";

// Gets the current user's details
export const GetUserURL = "/api/v1/user";

// Gets the current user's farms
export const GetFarmsURL = "/api/v1/farm/all";

// Gets farm's users
export const GetFarmUsersURL = "/api/v1/farm/users";

// Gets the details of a farm
export const GetFarmURL = "/api/v1/farm";

// Creates a farm
export const PostFarmURL = "/api/v1/farm";

// Adds a user to a farm
export const PostFarmUserURL = "/api/v1/farm/user";

// Gets the current user's farms' count
export const GetFarmsCountURL = "/api/v1/farm/all/count";

// Gets the current user's farm's sensors
export const GetFarmSensorsURL = "/api/v1/sensor/farm";

//Puts a user's new role
export const PutUserRoleURL = "/api/v1/farm/user";
